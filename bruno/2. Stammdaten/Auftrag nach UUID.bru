meta {
  name: Auftrag nach UUID
  type: http
  seq: 16
}

get {
  url: {{baseUrl}}/orders/{{orderId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Einzelner Auftrag per UUID. {{orderId}} stammt aus 'Aufträge synchronisieren'.
}
