meta {
  name: Rechnung nach UUID
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/invoices/{{invoiceId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Einzelne Rechnung per UUID. {{invoiceId}} stammt aus 'Rechnung erstellen'. 404, wenn nicht vorhanden.
}
